import { ModalComponent } from '../../baseComponents/Modal';

export const TimerConfigurationModal = ({ isOpen, handelModalClose }) => {
  return (
    <ModalComponent isOpen={isOpen} handelModalClose={handelModalClose}>
      <div>3113</div>
    </ModalComponent>
  )
}